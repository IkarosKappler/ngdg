/**
 * An AppContext function: set the global outline to use.
 *
 * @date 2026-03-13 Refactored from the global `index.js`.
 */
import { AppContext } from "../AppContext";
export declare const showSculptmap: (appContext: AppContext) => () => void;

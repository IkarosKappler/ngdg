/**
 * A helper to apply bumpmaps to any rectangular mesh.
 *
 * @author  Ikaros Kappler
 * @date    2021-09-06
 * @version 1.0.0
 */

import * as THREE from "three";

export const BumpMapper = {
  applyBumpmap: (geometry: any) => {}
};
